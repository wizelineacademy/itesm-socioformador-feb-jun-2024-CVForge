/*import prisma from '@/lib/prisma';

// Validar JWT (Jason Web Token)
// Nomenclatura: Usar Find
//CREATE, GET ALL, GET
export async function findUserByI(userId: string) {
  const user = await prisma.user.findUnique({
    where: { user_id: userId },
  });
  return user;
}*/